upstream authelia {
    server 127.0.0.1:9091;
}

# IP exacte de votre application SRV-WEB-01 vue dans vos captures
upstream webapp {
    server 172.16.33.20:8080;
}

server {
    listen 80;
    server_name portal.clinique-chatelet.local;
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name portal.clinique-chatelet.local;

    ssl_certificate /etc/nginx/ssl/portal.crt;
    ssl_certificate_key /etc/nginx/ssl/portal.key;

    # Protection globale du serveur proxy
    location /authelia {
        proxy_pass http://authelia;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }


    location = /authelia/api/verify {
        internal;
        proxy_pass http://127.0.0.1:9091/api/verify;
        proxy_pass_request_body off;
        proxy_set_header Content-Length "";
        
        # --- LE REMÈDE ANTI-BOUCLE INFINIE ---
        proxy_set_header Host $http_host;
        proxy_set_header X-Original-URL $scheme://$http_host$request_uri;
        
        proxy_set_header X-Forwarded-Method $request_method;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $http_host;
        proxy_set_header X-Forwarded-Uri $request_uri;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }

    # Redirection vers notre application Web Sécurisée
    location / {
        auth_request /authelia/api/verify;
        error_page 401 =302 https://$server_name/authelia/?rd=$scheme://$http_host$request_uri;
        
        include snippets/authelia-location.conf;
	
        proxy_pass http://webapp;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}