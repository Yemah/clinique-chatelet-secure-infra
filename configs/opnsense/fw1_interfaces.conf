# OPNsense FW1 — Interface Configuration (Anonymized)
# Command: ifconfig -a | grep -E "^[a-z]|inet "
# Tailscale IP and public-facing IPs redacted

vmx0 (WAN):          192.168.65.11/24        # WAN — towards Sophos UTM gateway
vmx1 (TRUNK):        (no IP — 802.1Q trunk)  # Parent interface for all VLANs
vmx2 (HA_SYNC):      10.0.0.1/30             # pfSync dedicated link → FW2 (10.0.0.2)
lo0 (Loopback):      127.0.0.1/8
pfsync0:             (active — state sync)
ovpns2 (OpenVPN):    10.100.100.1/24          # VPN tunnel — TOTP MFA required

# VLAN Sub-interfaces on vmx1 (trunk) — each with CARP VIP
vlan0.111 (SRV):     172.16.11.1/24    CARP vhid 11 → 172.16.11.254  [MASTER]
vlan0.222 (CLIENT):  172.16.22.1/24    CARP vhid 22 → 172.16.22.254  [MASTER]
vlan0.333 (DMZ):     172.16.33.1/24    CARP vhid 33 → 172.16.33.254  [MASTER]
vlan0.444 (BACKUP):  172.16.44.1/24    CARP vhid 44 → 172.16.44.254  [MASTER]
vlan0.555 (GUEST):   172.16.55.1/24    CARP vhid 55 → 172.16.55.254  [MASTER]
vlan0.999 (MGMT):    172.16.99.1/24    CARP vhid 99 → 172.16.99.254  [MASTER]

# Tailscale overlay (remote management — IP redacted)
tailscale0:          <REDACTED>/32            # Tailscale mesh VPN for OOB access
